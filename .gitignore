# Firebase Config (The file with your API keys)
src/firebase.js
src/firebaseConfig.js

# Environment Variables (The best practice)
.env
.env.local

# Firebase CLI cache and project aliases
.firebase/
.firebaserc
firebase-debug.log